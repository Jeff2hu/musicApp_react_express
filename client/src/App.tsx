// import { lazy, Suspense, useState } from "react";
// import ClickMe from "./ClickMe";
// const ClickMes = lazy(() => import("./ClickMe"));

function App() {
  // const [count, setCount] = useState(0);

  // const handleClick = () => {
  //   setCount(count + 1);
  // };

  return (
    <div>
      {/* <ClickMe count={count} handleClick={handleClick} />
      <Suspense fallback={<div>Loading...</div>}>
        <></>
      </Suspense> */}
      Hello world
    </div>
  );
}

export default App;
